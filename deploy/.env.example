# Secrets referenced by your config.yaml auth blocks (token_env / value_env).
# Copy this to `.env` and fill in real values:  cp .env.example .env
# The real .env is git-ignored so it never gets committed.

# SMARTOFFS_TOKEN=your-token-here
# PORTAINER_KEY=your-api-key-here
